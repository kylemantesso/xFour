#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running linting..."
pnpm lint || exit 1

echo "🏗️  Running build to ensure Vercel build will pass..."
pnpm build || exit 1

echo "✅ Pre-push checks passed!"

