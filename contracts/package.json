{
  "name": "@x402-gateway/contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia",
    "deploy:treasury": "hardhat run scripts/deploy-treasury-system.ts --network sepolia",
    "deploy:treasury:local": "hardhat run scripts/deploy-treasury-system.ts --network localhost",
    "verify:sepolia": "hardhat verify --network sepolia",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0",
    "typescript": "^5.0.0"
  }
}
