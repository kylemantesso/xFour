{
  "name": "x402-gateway",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @x402/web dev",
    "build": "pnpm --filter @x402/web build",
    "build:sdk": "pnpm --filter @x402/sdk build",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5"
  },
  "packageManager": "pnpm@9.14.2",
  "engines": {
    "node": ">=18"
  }
}
